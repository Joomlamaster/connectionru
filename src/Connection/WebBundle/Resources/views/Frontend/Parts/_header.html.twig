<div class="navbar navbar-first" role="navigation">
    <div class="container">
        <div class="navbar-header">
                {% if is_granted('ROLE_USER') %}
                {% else %}
                    {{ render(controller("ConnectionUserBundle:Facebook:facebookLoginButton")) }}
                    {{ render(controller("ConnectionUserBundle:Twitter:twitterLoginButton")) }}
                {% endif %}
        </div>
        <div class="navbar-collapse collapse">
            {% if is_granted('ROLE_USER') %}
                <h4 class="navbar-form navbar-right" style="color:#fff;">
                    Hello <a href="{{ path('edit_user_profile') }}">{{ app.user.username }}</a>
                    <a href="{{ path('fos_user_security_logout') }}">| Logout</a>
                </h4>
                <p style="color: #ffffff" class="navbar-form navbar-right">
                    <a href="{{ path('message_inbox') }}">Messages <span class="badge">{{ fos_message_nb_unread() }}</span></a>
                </p>

            {% elseif is_granted('IS_AUTHENTICATED_ANONYMOUSLY') %}
                {{ render(controller("ConnectionUserBundle:Security:login", {'request': app.request})) }}
            {% endif %}
        </div>
    </div>
</div>