<div class="navbar navbar-second" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('connection_homepage') }}">
                <div class="sprite sprite-logo-brand"></div>
            </a>
        </div>
        <div class="navbar-collapse collapse">
            <form class="navbar-form navbar-right" role="form">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('connection_homepage') }}">Home</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('connection_homepage') }}">Admin</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Events <span class="caret"></span></a>
                            <ul class="dropdown-menu" role="menu">
                                <li><a href="{{ path('event_manage') }}">+ Add event</a></li>
                            </ul>
                        </li>
                    {% endif %}
                    <li class="dropdown search-item">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-search"></span> Search <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('user_search') }}">Users</a></li>
                            <li><a href="{{ path('event_search') }}">Events</a></li>
                        </ul>
                    </li>
                    {% if not is_granted('ROLE_USER') %}
                        <li><a href="#">Register</a></li>
                    {% endif %}
                </ul>
            </form>
        </div>
    </div>
</div>
